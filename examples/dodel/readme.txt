In demo, fmincon is used with the wrapper functions. In demo_adjoint, adjoint
formulation is used to present the first-order necessary conditions to local
stationary points. Then successive continuation method is applied to find the
solutions to the necssary conditions.